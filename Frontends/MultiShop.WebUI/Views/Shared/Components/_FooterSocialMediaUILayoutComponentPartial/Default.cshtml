@model List<ResultSocialMediaDto>
<h6 class="text-secondary text-uppercase mt-4 mb-3">Bizi takip edin</h6>
<div class="d-flex">
    @foreach (var item in Model)
    {
        <a class="btn btn-primary btn-square mr-2" href="@item.Link"><i class="@item.Icon"></i></a>
    }
    
</div>
